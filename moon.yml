tasks:
  lint:
    args: --ignore-pattern 'templates/**'
    options:
      mergeArgs: prepend
  fmt:
    command: oxfmt --check .
  fmt.fix:
    command: oxfmt --write .
    options:
      runInCI: false
